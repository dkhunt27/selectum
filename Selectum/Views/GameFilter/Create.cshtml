@model Selectum.Models.GameFilter

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>GameFilter</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.GameFilterName)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.GameFilterName)
            @Html.ValidationMessageFor(model => model.GameFilterName)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.GameFilterStartDate)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.GameFilterStartDate)
            @Html.ValidationMessageFor(model => model.GameFilterStartDate)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.GameFilterEndDate)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.GameFilterEndDate)
            @Html.ValidationMessageFor(model => model.GameFilterEndDate)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
